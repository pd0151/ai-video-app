import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourname.viralcaption',
  appName: 'viralcaption ai',
  webDir: 'public'
};

export default config;
